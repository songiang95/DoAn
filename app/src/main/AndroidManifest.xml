<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.songiang.readebookandmanga">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoTitleBar">
        <activity android:name=".ebook.reading.ReadEbookOfflineActivity"></activity>
        <activity android:name=".ebook.favorite.FavoriteEbookActivity" />
        <activity android:name=".comic.favorite.FavoriteActivity" />
        <activity android:name=".ebook.search.SearchEbookActivity" />
        <activity android:name=".ebook.reading.ReadEbookActivity" />
        <activity android:name=".ebook.main.MainEbookActivity" />
        <activity android:name=".comic.search.SearchActivity" />
        <activity android:name=".comic.reading.ReadComicActivity" />
        <activity android:name=".comic.detail.DetailActivity" />
        <activity
            android:name=".comic.main.MainActivity"
            android:configChanges="keyboard|keyboardHidden|mcc|mnc|orientation|screenSize|locale|layoutDirection|smallestScreenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>